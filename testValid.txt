 /*
        String username = user.getUsername();
        String email = user.getEmail();

        for(int i = 0; i < getAll().size(); i++){

            if((getUser(i).username).equals(username) || (getUser(i).email).equals(email)){
                    System.out.println("Username or Email already exist");
            }else{

                 repository.save(user);
            }
        } */
           

      /* verificar que o user/ email ainda nao existe
        if (repository.findByUsername(username) != null && repository.findByEmail(email) != null) {
           repository.save(user);

        }else{
            System.out.println("ERROOOOOOOOOOO");
        } */