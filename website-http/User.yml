info:
  name: User
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/user/
  body:
    type: json
    data: |-
      {
        "username": "maria",
        "password": "palavrapasse",
        "email": "maria@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
