info:
  name: User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/user
  body:
    type: json
    data: |-
      {
        "username": "nome",
        "password": "palavrapasse",
        "email": "mar@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
